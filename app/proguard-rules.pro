# XX-Note release rules.
#
# Intentionally a stub for WS1. Rules arrive with the workstreams that add
# the libraries that need them: Room + FTS (WS3), OkHttp / WebDAV (WS4),
# WorkManager sync engine + Gson backup (WS5).
