<?xml version="1.0" encoding="utf-8"?>
<!-- Hardening #4b: nothing this app sees leaves the device by Google's hand.
     Every storage domain is excluded from BOTH channels on Android 12+:
     cloud backup (Google Drive via Play Services) and device-to-device
     transfer (cable/QR setup restore). Belt-and-braces next to
     android:allowBackup="false" in the manifest — if that flag is ever
     relaxed, these rules still keep filesDir/vault (every note), the Room
     database holding the sealed credential blob, and shared prefs out.
     path="." excludes the whole domain; "root" alone would already cover
     them all, but each domain is spelled out so intent survives auditing. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="."/>
        <exclude domain="database" path="."/>
        <exclude domain="sharedpref" path="."/>
        <exclude domain="file" path="."/>
        <exclude domain="external" path="."/>
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="."/>
        <exclude domain="database" path="."/>
        <exclude domain="sharedpref" path="."/>
        <exclude domain="file" path="."/>
        <exclude domain="external" path="."/>
    </device-transfer>
</data-extraction-rules>
