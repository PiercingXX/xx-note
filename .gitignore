.gradle/
build/
local.properties
.idea/
*.iml
.kotlin/

# Skippy runtime bookkeeping — never tracked
.skippy/

# Estate mill/audit scratch — never tracked
.nagatha/
.bilby/

# Mill-seeded venv is a house symlink or share dir — never tracked
.venv
